package test;

import foo.*;

class Asdf
{
  uint a;
  ? :Asdf next;
  Asdf(:Asdf other)
  {
    a = 15;
    next = other;
  }
  Asdf(uint b)
  {
    a = b;
    next = null;
  }

  uint getA()
  {
    return a;
  }

  static :bar.complex process(:bar.complex c)
  {
    :bar.complex argh = bar.complex(12, 5);
    return :bar.complex::rotateLeft(c);
  }
}

